name: Repo Bot

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run bot script
        run: |
          echo "Bot is running"
